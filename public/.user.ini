; Per-directory PHP overrides (Apache / FastCGI with user_ini enabled).
; Ignored by `php artisan serve` — use: php -d max_file_uploads=64 artisan serve
max_file_uploads = 64
upload_max_filesize = 64M
post_max_size = 256M
